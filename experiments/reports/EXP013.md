# EXP013 — Gemma4-E4B QLoRA finetune training on Lightning A100 (produces v2-fresh adapter_best)

## Status

VALID

## Executive Summary

Lightning AI training run (1x A100-80GB, 15/15 cells executed) that QLoRA-finetuned google/gemma-4-E4B-it (r64/alpha128, LR 1e-4, max 8 epochs + early stopping patience 3) on 8200 train / 1800 val and pushed adapter_best to ShivRamSaud/gemma4-e4b-qlora-astroclimb-v2-fresh. Best eval_loss 0.3464 at step-1000 (~1.95ep inferred); run continued to ~step-1600 (~3.1ep inferred). This adapter is the artifact behind EXP010 and the official 0.64282 winning submission.

## Research Question

Can QLoRA finetuning of Gemma4-E4B on OLD-format 10k pairs produce an adapter that wins on the official test set?

## Model

google/gemma-4-E4B-it

## Model Version

unknown

## Dataset

ShivRamSaud/astroclimb_train via Hugging Face (OLD train.csv 10k, base64 obj_1/obj_2); local origin /teamspace/studios/this_studio/train (1).zip.

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (no version pin recorded in-notebook)

## Dataset Configuration

train 8200 + val 1800 balanced (seed 42, same split as SmolVLM/GLM runs).

## Data Split

train 8200 / val 1800 (loss-monitored; no generation eval in-notebook).

## Task Formulation

Multiclass single-label 4-class pair classification (same contract as all runs). Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers (answer-only supervision; prompt tokens masked).

## Input Modality

image + caption (figure images base64-decoded, captions truncated).

## Preprocessing

Base64 decode + thumbnail(448,448); answer-only loss masking.

## Training Method

QLoRA SFT, single A100-80GB, Lightning cloudspace kernel.

## Full Training Configuration

epochs_max 8; steps/epoch 512; total planned ~4100; actual ~1600 (~3.1ep inferred); best eval_loss 0.34641456604003906 at checkpoint-1000 (~1.95ep inferred); eval every 200 steps; early stopping patience 3; batch 2, grad_accum 8, eff 16; LR 1e-4; LoRA r64/alpha128/dropout 0.05 on q/k/v/o/gate/up/down; bf16; 4-bit NF4; max_seq_len 1536; max_steps cap 2560; torch 2.8.0+cu128, transformers 5.16.1, bitsandbytes 0.50.2, peft 0.20.0, trl 1.12.0. Optimizer/scheduler/warmup/seed: unknown (not extracted).

## Inference Configuration

None (training notebook; generation eval lives in EXP001/EXP010).

## Prompting Strategy

Zero-shot descriptive (same contract), answer-only supervision.

## Exact System Prompt

Same descriptive prompt as EXP001 (see experiments/prompts/EXP001_system.txt); training uses the identical builder with loss masking.

## Exact User Prompt

Same object-block template (image-before-text, captions truncated) + gold label as assistant target.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

None during training (label supervision).

## Output Format

Lowercase label only.

## Prediction Parsing

Not applicable (loss training; no generation parsing in-notebook).

## Evaluation Method

Eval-loss monitoring every 200 steps on val 1800 (NOT macro-F1). No generation metric in-notebook.

## Local Metrics

Best eval_loss 0.34641456604003906 at step-1000. No macro-F1 in this notebook (see EXP001: 0.6170 NEW-val for the v1 lineage; EXP010 for v2-fresh test inference).

## Official Metrics

Not applicable (training only). Downstream: EXP010 test submission with this adapter lineage scored official 0.64282 (user-confirmed).

## Results

Training completed; best adapter pushed to ShivRamSaud/gemma4-e4b-qlora-astroclimb-v2-fresh/adapter_best (+ intermediate ckpts 1200, 1600). Merged push skipped in-session.

## Artifacts

HF: ShivRamSaud/gemma4-e4b-qlora-astroclimb-v2-fresh/adapter_best (+ ckpts 1200/1600). Config snapshot: configs/EXP013.yaml.

## Notebook Provenance

Single notebook (lightning_gemma_e4b_qlora_80gb (1) (1).ipynb), cloudspace kernel, 15/15 cells executed, added by user after first audit.

## Version Reconstruction

No reconstruction. Forward links: EXP001 evaluates the v1 adapter lineage on NEW data; EXP010 submits the v2-fresh adapter on test.

## Known Errors

Epoch-reporting discrepancy (unresolved, documented): EXP010 notebook says adapter_best "best at 3.1ep"; session log shows best LOSS at step-1000 (~1.95ep) with the run continuing to ~step-1600 (~3.1ep); user describes the winning model as "3.5ep". Treated as the same run with approximate epoch reporting; HF adapter_best is mutable and may postdate the session. Do not harmonize by guessing.

## Known Limitations

No generation macro-F1 in-notebook; optimizer/scheduler/warmup/seed unknown; HF artifact mutability means the current adapter_best may differ from the session's push.

## Reproducibility

Needs Lightning/cloud A100-80GB (or equivalent), HF dataset ShivRamSaud/astroclimb_train, config in configs/EXP013.yaml; ~1.1h estimated (0.56h for 5ep + val).

## Official Confirmation Status

confirmed by user that this run produced the finetuned model behind the 0.64282 official submission.

## Evidence / Confidence

high — executed training cells, loss log, and HF push receipts tie the artifact to the implementation.
