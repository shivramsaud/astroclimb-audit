# EXP012 — SmolVLM-500M LoRA adapter submission (test 10k, completed, anomalous distribution)

## Status

VALID

## Executive Summary

Completed but ANOMALOUS: zero unrelated_papers out of 10k (vs 4253 in EXP010, 3396 in EXP011) suggests collapsed/biased decoding. Do not trust without official score.

## Research Question

Can the SmolVLM LoRA adapter score test? (Answer: yes, but distribution is suspect.)

## Model

HuggingFaceTB/SmolVLM-500M-Instruct + ShivRamSaud/smolvlm-500m-lora-astroclimb/adapter_best (best eval_loss 0.30)

## Model Version

unknown

## Dataset

/kaggle/input/competitions/astroclimb/test.csv

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

10000 rows

## Data Split

Official test inference, no local metric possible

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

OLD-format test reader (base64), thumbnail(448,448); LoRA adapter attached.

## Training Method

LoRA r32 fp16 finetune trained in excluded train-only notebooks (19.1M params, 3.63%); this notebook = inference

## Full Training Configuration

Train (external to this notebook): LoRA r32, fp16 (T4 no BF16), MAX_SEQ_LEN 2048, batch 1x16 eff 16, ckpt 400/800, best loss 0.3029. See excluded train notebook.

## Inference Configuration

max_new_tokens=32, do_sample=False, T4 x2; generate check OK (sample raws 'same_paper.').

## Prompting Strategy

Zero-shot descriptive (train-exact contract) -> one-hot.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP012_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

None.

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

No local metric. Completion: 10000/10000, oom=0. Distribution: same_paper 6884, related 2211, same_figure 905, unrelated_papers 0.

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

Completed but ANOMALOUS: zero unrelated_papers out of 10k (vs 4253 in EXP010, 3396 in EXP011) suggests collapsed/biased decoding. Do not trust without official score.

## Artifacts

HF ShivRamSaud/smolvlm-500m-lora-astroclimb/submission/submission.csv (+ progress + raw_test.jsonl).

## Notebook Provenance

Inference here; training in excluded smolvlmfinetune notebooks (SyntaxError blocked their in-notebook eval). No metric merge possible (test unlabeled).

## Version Reconstruction

Not merged as VALID_RECONSTRUCTED (no shared evaluated artifact with metric).

## Known Errors

Train-notebook eval SyntaxError (excluded); test distribution anomaly (this notebook).

## Known Limitations

Anomalous distribution; training/eval split lives across notebooks; official score unknown.

## Reproducibility

Re-runnable on T4 x2 with test.csv + HF adapter.

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

medium — completion certain, quality suspect (0/10000 unrelated).
