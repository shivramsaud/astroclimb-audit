# EXP003 — Gemma4-E4B few-shot thinking eval (filename said baseline-qwen)

## Status

VALID

## Executive Summary

Directly comparable to the EXP002 zero-shot baseline (same OLD-1800 eval): few-shot thinking scores 0.3925 vs 0.4408 zero-shot.

## Research Question

Does few-shot thinking help Gemma4-E4B on sampled pairs?

## Model

google/gemma-4-E4B-it (code MODEL_ID; filename baseline-qwen is WRONG)

## Model Version

unknown

## Dataset

OLD train.csv for evaluation (/kaggle/input/competitions/astroclimb/train.csv, 10000x7 base64) + NEW 23-col sampled data as few-shot-example source only. Executed outputs state: 'EVALUATION DATASET: OLD train.csv balanced 1800 (450 per class)' and 'FEW-SHOT SOURCE: NEW dataset (23-col, UUID images) -> 4 examples injected as user/assistant turns'. (Correction 2026-09-14 re-verification: earlier drafts misread the dual-reader setup as NEW-data evaluation; the OLD grouping TXT-IMG/IMG-IMG/TXT-TXT in the metrics confirms OLD eval.)

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

Eval: OLD 1800 balanced (450/label; TXT-IMG 900 / IMG-IMG 450 / TXT-TXT 450). Demos: 4 NEW examples (1/class, seed 42, preferring FIG-CAP/CAP-FIG), injected as user/assistant turns

## Data Split

Full OLD-1800 few-shot thinking eval (no train split); NEW data never evaluated, only quoted as demos

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

NEW sampled UUID image load + thumbnail(448,448); captions truncated; 4 few-shot demo blocks prepended.

## Training Method

None (no training)

## Full Training Configuration

N/A - zero-shot.

## Inference Configuration

temperature never set in code (greedy via do_sample=False); thinking on (ENABLE_THINKING=True); max_new_tokens=512, batch 1, T4 x2, elapsed 6.26h/6.5h.

## Prompting Strategy

Few-shot (1/class = 4 demos, seed 42, prefer FIG-CAP/CAP-FIG) + descriptive prompt.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP003_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

4 examples (1 per class, seed 42).

## Reasoning Strategy

Native thinking enabled.

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.5050, macro-F1=0.3925, n=1800. Per-class: same_figure 0.7228, same_paper 0.2047, related_papers 0.0505, unrelated_papers 0.5918.

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

Directly comparable to the EXP002 zero-shot baseline (same OLD-1800 eval): few-shot thinking scores 0.3925 vs 0.4408 zero-shot.

## Artifacts

preds_train_bal_final.npy, metrics.json, confusion.png, raw_outputs.jsonl.

## Notebook Provenance

Single notebook (baseline-qwen (1).ipynb); filename contradicts code - rename fixes this.

## Version Reconstruction

No reconstruction.

## Known Errors

Dataset-identity ambiguity (markdown OLD vs code NEW path); flagged, not resolved by guessing.

## Known Limitations

Metric not comparable to EXP002 (different rows/format); thinking-trace parsing details unknown.

## Reproducibility

Re-runnable on T4 x2; resolve dataset path first (code path = NEW sampled).

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

high — full-1800 metric from executed cells; hybrid OLD-eval/NEW-demo design confirmed by executed outputs.
