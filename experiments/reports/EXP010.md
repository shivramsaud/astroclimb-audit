# EXP010 — Gemma4-E4B v2-fresh adapter submission (test 10k, completed)

## Status

VALID

## Executive Summary

Completed test submission: 10000/10000 to HF v2-fresh repo. Distribution: unrelated 4253, same_paper 3005, related 2058, same_figure 684.

## Research Question

Can the v2-fresh adapter score the full 10k test? (Answer: yes, 10000/10000.)

## Model

google/gemma-4-E4B-it + ShivRamSaud/gemma4-e4b-qlora-astroclimb-v2-fresh/adapter_best (best at 3.1ep)

## Model Version

unknown

## Dataset

/kaggle/input/competitions/astroclimb/test.csv (official test, obj_1/obj_2 only, no labels)

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

10000 rows

## Data Split

Official test inference (no local ground truth; no local metric possible)

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

Test reader dual-format (base64 image vs caption text), thumbnail(448,448), missing -> skip (0 skipped).

## Training Method

QLoRA adapter trained externally (3.1ep); this notebook = resumable inference + submission

## Full Training Configuration

N/A (inference-only notebook).

## Inference Configuration

max_new_tokens=32, do_sample=False, 4-bit NF4, device_map auto {0:14GB,1:14GB}, T4 x2, resumable via HF progress (resumed 5979).

## Prompting Strategy

Zero-shot descriptive (same contract) -> one-hot submission columns.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP010_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

None (greedy label).

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

No local metric (test labels unavailable). Completion: 10000/10000 scored, skipped=0, oom=0, parse_fb=0.

## Official Metrics

Official test score (user-confirmed, leaderboard Image 1): **0.64282** (Score column; presumed macro-F1, not independently verified). Highest of the four official submissions; selected entry (blue highlight).
Attribution: v2-fresh adapter_best (EXP013 Lightning run; EXP010 notebook labels it best at 3.1ep; user describes 3.5ep; session log shows run to ~step-1600 ~3.1ep — same run, approximate epoch reporting).

## Results

Completed test submission: 10000/10000 to HF v2-fresh repo. Distribution: unrelated 4253, same_paper 3005, related 2058, same_figure 684.

## Artifacts

HF ShivRamSaud/gemma4-e4b-qlora-astroclimb-v2-fresh/submission/: submission_progress.csv, raw_test.jsonl, submission.csv (local staging deleted; HF copy is deliverable).

## Notebook Provenance

Single notebook (3-5gemmae4bsubmission (1).ipynb); twin (2).ipynb failed (4021 unscored) in excluded/.

## Version Reconstruction

No metric reconstruction (test has no labels).

## Known Errors

Twin run hit 4021-unscored AssertionError - this primary succeeded.

## Known Limitations

No local metric by construction; distribution plausibility unvalidated; official score unknown.

## Reproducibility

Re-runnable on T4 x2 with test.csv + HF adapter; download HF submission.csv -> upload to competition.

## Official Confirmation Status

confirmed by user (leaderboard screenshot Image 1 + text mapping: highest = 3.5ep e4b gemma trained on Lightning, submission generated by testing test files on Kaggle).

## Evidence / Confidence

high for inference completion; no metric claimed.
