# EXP004 — Qwen3-VL-8B-Thinking zero-shot (OLD)

## Status

VALID

## Executive Summary

Degenerate: model predicts unrelated_papers almost always (macro-F1 0.1068). Thinking mode did not help this setup.

## Research Question

Can Qwen3-VL-8B-Thinking zero-shot classify OLD-format pairs? (Answer: effectively no, in this setup.)

## Model

Qwen/Qwen3-VL-8B-Thinking

## Model Version

unknown

## Dataset

/kaggle/input/competitions/astroclimb/train.csv (OLD 7-col)

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

1800 balanced (450/label)

## Data Split

Balanced-subset eval 1800/1800; trailing viz cell crashed (IndexError 1800 vs 10000) AFTER metrics - metric unaffected

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

OLD base64 decode + thumbnail; captions [:2000].

## Training Method

None (zero-shot)

## Full Training Configuration

N/A.

## Inference Configuration

Greedy (do_sample=False), max_new_tokens=32, batch 1, 4-bit NF4, device_map auto {0:13GB,1:13GB}, T4 x2.

## Prompting Strategy

Zero-shot descriptive, label-only.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP004_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

Native <think> reasoning, single generation.

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.2461, macro-F1=0.1068, n=1800. Per-class: same_figure 0.0322, same_paper 0.0, related_papers 0.0, unrelated_papers 0.3949 (collapsed to unrelated_papers).

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

Degenerate: model predicts unrelated_papers almost always (macro-F1 0.1068). Thinking mode did not help this setup.

## Artifacts

preds npy, metrics.json (plots incomplete due to viz crash).

## Notebook Provenance

Primary baseline-qwen.ipynb; twin baseline-qwen (2).ipynb (macro 0.1060) superseded in notebooks/excluded/.

## Version Reconstruction

No train/eval merge (single-stage eval).

## Known Errors

Final plotting cell IndexError (boolean index 1800 vs 10000) - cosmetic, after metrics.

## Known Limitations

Collapsed predictions; OLD data only; prompt/model mismatch suspected (wrong AutoModel class history per AGENTS.md).

## Reproducibility

Re-runnable on T4 x2 with OLD train.csv.

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

high for the metric itself (executed eval cells); low for model-quality claims (degenerate).
