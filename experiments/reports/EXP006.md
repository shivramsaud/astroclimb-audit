# EXP006 — Kimi-VL-A3B-Thinking few-shot (NEW, all-OOM degenerate)

## Status

VALID

## Executive Summary

Total OOM collapse: 1800/1800 fell back to unrelated_papers. Metric documents that Kimi-A3B-Thinking (this config) cannot run on T4 x2 here.

## Research Question

Can Kimi-VL-A3B-Thinking run few-shot on T4 x2 at this resolution? (Answer: no.)

## Model

moonshotai/Kimi-VL-A3B-Thinking (modeling_kimi_vl.py patched at runtime)

## Model Version

unknown (snapshot 7d99e220...)

## Dataset

jcxiv42 sampled NEW 23-col

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

1800 balanced, full 1800 scored (all via OOM fallback)

## Data Split

Full-1800 eval but every generation OOM'd; metric records the failure mode

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

NEW UUID image load attempted; OOM on every generation so fallback path dominated.

## Training Method

None

## Full Training Configuration

N/A.

## Inference Configuration

T4 x2, elapsed 4.2h; effectively every sample OOM -> fallback unrelated_papers.

## Prompting Strategy

Few-shot 4 demos + thinking (never successfully generated).

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP006_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

4 examples defined; generation OOM'd anyway.

## Reasoning Strategy

Intended native <think>; unrealized (OOM).

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.2500, macro-F1=0.1000, n=1800. Per-class: only unrelated_papers 0.40, rest 0.0 (single-label collapse).

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

Total OOM collapse: 1800/1800 fell back to unrelated_papers. Metric documents that Kimi-A3B-Thinking (this config) cannot run on T4 x2 here.

## Artifacts

preds npy, metrics.json, raw_outputs.jsonl (OOM logs), plots.

## Notebook Provenance

Single notebook (loading-encoded-astroclimb-images (3).ipynb).

## Version Reconstruction

No reconstruction.

## Known Errors

Hundreds of OOM lines; modeling file runtime-patched (fragile).

## Known Limitations

Says nothing about Kimi quality - only about this OOM'd configuration.

## Reproducibility

Needs smaller memory footprint (quantization/resolution) before any quality claim.

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

medium — completed eval of a failed configuration; honest degenerate metric.
