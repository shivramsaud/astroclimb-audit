# EXP009 — GLM-4.6V-Flash zero-shot (OLD, truncated, degenerate)

## Status

VALID

## Executive Summary

OOM-forced bf16 + slow decoding = 610/1800, all unrelated_papers. Configuration unviable on T4 x2 as run.

## Research Question

Can GLM-4.6V-Flash zero-shot run on T4 x2 at this setting? (Answer: not usefully.)

## Model

zai-org/GLM-4.6V-Flash (Glm4vForConditionalGeneration; Has generate True, Has chat False)

## Model Version

unknown

## Dataset

/kaggle/input/competitions/astroclimb/train.csv (OLD)

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

1800 target; 610 scored before 6.5h guard

## Data Split

Truncated 610/1800 + degenerate (all unrelated_papers); trailing viz IndexError after metrics

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

OLD base64 decode + thumbnail; bf16 fallback after 4-bit OOM.

## Training Method

None

## Full Training Configuration

N/A.

## Inference Configuration

temperature never set in code (greedy via do_sample=False); thinking off (ENABLE_THINKING=False); max_new_tokens=32, do_sample=False, batch 1, bf16 sharded (4-bit OOM observed in executed outputs, fell back), T4 x2, guard break.

## Prompting Strategy

Zero-shot descriptive.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP009_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

None.

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.2557, macro-F1=0.1020 on scored 610. Per-class: only unrelated_papers 0.4078, rest 0.0.

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

OOM-forced bf16 + slow decoding = 610/1800, all unrelated_papers. Configuration unviable on T4 x2 as run.

## Artifacts

preds npy (610), metrics.json.

## Notebook Provenance

Single notebook (loading-encoded-astroclimb-images (8).ipynb).

## Version Reconstruction

No reconstruction.

## Known Errors

4-bit OOM then bf16 fallback; guard truncation; viz IndexError (610 vs 1800).

## Known Limitations

Degenerate + truncated: weakest evidence in the registry.

## Reproducibility

Needs quantization + resolution fixes before re-attempt.

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

low — metric is an honest record of a failed configuration, not a quality estimate.
