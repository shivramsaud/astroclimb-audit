# EXP008 — GLM-4.1V-9B-Thinking few-shot (NEW, truncated)

## Status

VALID

## Executive Summary

Best truncated run (0.42 on 445), but subset-only: same_figure strong, related_papers zero.

## Research Question

Can GLM-4.1V-9B-Thinking few-shot beat baselines on NEW pairs? (Unfinished: 445/1800.)

## Model

Rainnighttram/GLM-4.1V-9B-Thinking-bnb-4bit (9B pre-quantized)

## Model Version

unknown

## Dataset

jcxiv42 sampled NEW 23-col

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

1800 target; 445 scored before 6.52h guard

## Data Split

Truncated eval 445/1800 (111/115/107/112) - subset metric

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

NEW UUID image load + thumbnail(448,448); 4 few-shot demos; bnb-4bit thinking model.

## Training Method

None in this notebook (separate train-only notebooks excluded)

## Full Training Configuration

N/A here (see excluded GLM train-only notes for LoRA r16 DDP config).

## Inference Configuration

do_sample=False, batch 1, pre-quantized bnb-4bit, T4 x2, 6.52h guard break.

## Prompting Strategy

Few-shot 4 demos + thinking.

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP008_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

4 examples (1/class).

## Reasoning Strategy

Native <think>.

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.5236, macro-F1=0.4200 on scored 445. Per-class: same_figure 0.8653, same_paper 0.2571, related_papers 0.0, unrelated_papers 0.5575.

## Official Metrics

pending user confirmation — no official Kaggle/shared-task score established from repository evidence.

## Results

Best truncated run (0.42 on 445), but subset-only: same_figure strong, related_papers zero.

## Artifacts

preds_train.npy, preds_train_bal_final.npy, raw_outputs.jsonl, metrics.json, plots.

## Notebook Provenance

Single notebook (loading-encoded-astroclimb-images (7).ipynb). NOT merged with train-only notebooks (different method).

## Version Reconstruction

No merge (incompatible: eval-only vs train-only, no shared adapter artifact evaluated).

## Known Errors

6.52h guard truncation.

## Known Limitations

Subset metric; related_papers unclassified even in subset.

## Reproducibility

Needs longer budget to complete 1800.

## Official Confirmation Status

pending user confirmation (see README section Official Test Results and Submission Confirmation).

## Evidence / Confidence

medium — honest subset metric.
