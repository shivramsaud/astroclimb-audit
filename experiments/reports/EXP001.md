# EXP001 — Gemma4-E4B QLoRA finetuned adapter evaluation (NEW 23-col)

## Status

VALID

## Executive Summary

Best local result in the directory (macro-F1 0.6170). Old-format 0.4408 baseline is different rows and not comparable.

## Research Question

Does QLoRA finetuning on the NEW 23-col data beat zero-shot baselines for 4-class figure-caption relation classification?

## Model

google/gemma-4-E4B-it + ShivRamSaud/gemma4-e4b-qlora-astroclimb/adapter_best (merged in-notebook to merged_best)

## Model Version

unknown

## Dataset

jcxiv42/astroclimb sampled: astroclimb_4class(sampled).csv + object_images_sampled/{uuid}.png

## Dataset URL

https://huggingface.co/datasets/adsabs/AstroCLIMB

## Dataset Version

unknown (Kaggle snapshot; no version pin recorded in-notebook)

## Dataset Configuration

1800 rows, 23 cols, balanced 450/label; pair types FIG-CAP/CAP-FIG/FIG-FIG/CAP-CAP 450 each

## Data Split

Generation eval over all 1800 (adapter trained externally; no in-notebook train split)

## Task Formulation

Multiclass single-label classification of an (Object A, Object B) pair into exactly one of 4 relations. Competition: https://www.kaggle.com/competitions/astroclimb | Shared task: https://ui.adsabs.harvard.edu/WIESP/2026/shared_task | Dataset: https://huggingface.co/datasets/adsabs/AstroCLIMB.

## Labels

same_figure, same_paper, related_papers, unrelated_papers. NEW 23-col ints: 0=same_paper, 1=related_papers, 2=same_figure, 3=unrelated_papers. Submission column order: same_figure, same_paper, related_papers, unrelated_papers.

## Input Modality

image + caption (figure images and caption text, either side can be either)

## Preprocessing

Figure: extract UUID from object_*_content path, load object_images_sampled/{uuid}.png, thumbnail(448,448). Caption: text truncate [:2000]. Missing figure -> skip row (0 skipped).

## Training Method

QLoRA-finetuned generation eval (adapter trained outside this notebook; evaluated + merged here)

## Full Training Configuration

Finetune-train: 900 steps, best val_loss 0.3489 (per notebook output). Other hyperparameters unknown (not recorded here). Eval-side: 4-bit NF4, bfloat16 compute, double_quant True, device_map auto {0:14GB,1:14GB}.

## Inference Configuration

Greedy via do_sample=False (temperature never set in code), max_new_tokens=32, batch 1, Kaggle GPU T4 x2.

## Prompting Strategy

Zero-shot descriptive (same contract as finetune input: image-before-text).

## Exact System Prompt

Exact system prompt preserved in experiments/prompts/EXP001_system.txt (verbatim descriptive prompt; see Prompt files).

## Exact User Prompt

SYSTEM_PROMPT + per-object blocks: image thumbnail(448,448) as 'Object A/B: [Figure image]' or caption text[:2000] as 'Object A/B (caption): <text>', in pair order, + 'Answer with one label:'. Images placed BEFORE their text per the finetune contract in finetuned runs.

## Few-Shot Examples

No few-shot examples.

## Reasoning Strategy

None (direct label; greedy).

## Output Format

Lowercase label only (same_figure | same_paper | related_papers | unrelated_papers), no explanation, no punctuation.

## Prediction Parsing

Lowercase/strip normalization, map to the 4 labels; unparseable or OOM rows fall back to unrelated_papers (counts reported per run; 0 fallbacks in EXP001/EXP010/EXP011/EXP012).

## Evaluation Method

sklearn classification_report with labels=label_cols on the scored split; macro-F1 is the primary metric; per-class F1, accuracy, confusion matrix and per-pair-type F1 also recorded. Local only unless Official Metrics says otherwise.

## Local Metrics

acc=0.6239, macro-F1=0.6170, n=1800/1800, skipped=0. Per-class: same_figure 0.8169, same_paper 0.5926, related_papers 0.3715, unrelated_papers 0.6871. Per-pair: FIG-CAP 0.5862, CAP-FIG 0.5728, FIG-FIG 0.5805, CAP-CAP 0.5883.

## Official Metrics

Official test score (user-confirmed, leaderboard Image 1): **0.40088** (Score column; presumed macro-F1 per task metric, not independently verified).
Attribution: v1 adapter_best ~900 steps (~1.76 epochs, inferred from 900/512 steps-per-epoch) merged to merged_best — the user's "same model 1.8epoch trained" second-highest submission.
Local NEW-val macro-F1 for this lineage: 0.6170. Gap: -0.216 (distribution shift between sampled val and official test). Both figures preserved; neither overwrites the other.

## Results

Best local result in the directory (macro-F1 0.6170). Old-format 0.4408 baseline is different rows and not comparable.

## Artifacts

preds_val_final.npy (+ per-1000 checkpoints), metrics.json, confusion.png, per_class_f1.png, per_pair_f1.png, raw_outputs.jsonl + raw_outputs_final.jsonl, skipped.json; HF: eval/ + merged_best under ShivRamSaud/gemma4-e4b-qlora-astroclimb.

## Notebook Provenance

Train: external (adapter_best from HF). Eval+merge: this notebook end to end (generate check OK, full 1800 scored, merge pushed).

## Version Reconstruction

No reconstruction (single notebook completed).

## Known Errors

None blocking.

## Known Limitations

Adapter training hyperparameters (LR, rank, epochs) not recorded in this notebook; test generalization unknown.

## Reproducibility

Re-runnable given the two Kaggle input paths + HF adapter_best + transformers>=5.15.1 (notebook self-upgrades to 5.17.0.dev0) on T4 x2.

## Official Confirmation Status

confirmed by user (leaderboard screenshot Image 1 + text mapping: second-highest = same model 1.8ep).
Caveat: the in-directory merged_best submission notebooks (excluded twins) FAILED (3136 unscored); the completed submission session is not preserved locally. The score is matched to this adapter lineage (900-step v1 adapter, EXP001 eval) by converging evidence, not by a preserved completed run.

## Evidence / Confidence

high — metric directly produced by executed eval cells and tied to implementation + saved artifacts.
